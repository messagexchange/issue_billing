<p>
  <label for="ib_tracker_id">Tracker to bill</label>
  <%= text_field_tag 'settings[ib_tracker_id]', @settings['ib_tracker_id'] %>
</p>

<p>
  <label for="ib_non_billable_activity_ids">Non billable activities</label>
  <%= text_field_tag 'settings[ib_non_billable_activity_ids]', @settings['ib_non_billable_activity_ids'] %>
</p>

<p>
  <label for="ib_raised_by_id">Custom field id for Raised by value</label>
  <%= text_field_tag 'settings[ib_raised_by_id]', @settings['ib_raised_by_id'] %>
</p>

<p>
  <label for="ib_non_billable_custom_id">Custom field id non billable issue field</label>
  <%= text_field_tag 'settings[ib_non_billable_custom_id]', @settings['ib_non_billable_custom_id'] %>
</p>

<p>
  <label for="ib_logo_image">PDF logo path (must be an absolute path)</label>
  <%= text_field_tag 'settings[ib_logo_image]', @settings['ib_logo_image'], :style => 'width: 90%' %>
</p>

<p>
  <label for="ib_logo_height">PDF logo height</label>
  <%= text_field_tag 'settings[ib_logo_height]', @settings['ib_logo_height'] %>
</p>
